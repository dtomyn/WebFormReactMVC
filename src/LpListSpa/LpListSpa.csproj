<!-- Generated by GitHub Copilot -->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <EnableDefaultItems>false</EnableDefaultItems>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    <Nullable>enable</Nullable>
    <SpaDistDir>$(MSBuildProjectDirectory)\dist</SpaDistDir>
    <LegacyHostClientAppDir>$(MSBuildProjectDirectory)\..\LegacyHost.WebForms\ClientApp</LegacyHostClientAppDir>
    <ModernHostPeopleAppDir>$(MSBuildProjectDirectory)\..\ModernHost.Mvc\wwwroot\people-app</ModernHostPeopleAppDir>
  </PropertyGroup>
  <ItemGroup>
    <Compile Include="SolutionMarker.cs" />
    <None Include=".gitignore" />
    <None Include="eslint.config.js" />
    <None Include="index.html" />
    <None Include="package-lock.json" />
    <None Include="package.json" />
    <None Include="README.md" />
    <None Include="vite.config.js" />
    <None Include="public\**\*" />
    <None Include="src\**\*" />
  </ItemGroup>
  <Target Name="EnsureNodeModules" BeforeTargets="Build" Condition="!Exists('$(MSBuildProjectDirectory)\node_modules')">
    <Exec Command="npm install" WorkingDirectory="$(MSBuildProjectDirectory)" />
  </Target>
  <Target Name="BuildSpa" BeforeTargets="Build">
    <Exec Command="npm run build" WorkingDirectory="$(MSBuildProjectDirectory)" />
  </Target>
  <Target Name="SyncSpaToHosts" AfterTargets="Build" Condition="Exists('$(SpaDistDir)')">
    <ItemGroup>
      <BuiltSpaAssets Include="$(SpaDistDir)\**\*" />
    </ItemGroup>
    <RemoveDir Directories="$(LegacyHostClientAppDir)" Condition="Exists('$(LegacyHostClientAppDir)')" />
    <RemoveDir Directories="$(ModernHostPeopleAppDir)" Condition="Exists('$(ModernHostPeopleAppDir)')" />
    <MakeDir Directories="$(LegacyHostClientAppDir)" />
    <MakeDir Directories="$(ModernHostPeopleAppDir)" />
    <Copy SourceFiles="@(BuiltSpaAssets)" DestinationFiles="@(BuiltSpaAssets->'$(LegacyHostClientAppDir)\%(RecursiveDir)%(Filename)%(Extension)')" />
    <Copy SourceFiles="@(BuiltSpaAssets)" DestinationFiles="@(BuiltSpaAssets->'$(ModernHostPeopleAppDir)\%(RecursiveDir)%(Filename)%(Extension)')" />
  </Target>
  <Target Name="CleanSpa" AfterTargets="Clean">
    <RemoveDir Directories="$(SpaDistDir)" Condition="Exists('$(SpaDistDir)')" />
    <RemoveDir Directories="$(LegacyHostClientAppDir)" Condition="Exists('$(LegacyHostClientAppDir)')" />
    <RemoveDir Directories="$(ModernHostPeopleAppDir)" Condition="Exists('$(ModernHostPeopleAppDir)')" />
  </Target>
</Project>