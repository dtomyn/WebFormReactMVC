@* Generated by GitHub Copilot *@
@model HomePageViewModel
@{
    ViewData["Title"] = "People Directory";
}

@section Styles {
    <link rel="stylesheet" href="~/people-app/people-app.css" asp-append-version="true" />
}

<section class="hero" aria-label="Host overview">
    <p class="hero__eyebrow">Phase 2</p>
    <p class="hero__summary">
        This MVC page hosts the shared React people directory bundle and passes the configured Modern API base URL through its mount attributes.
    </p>
</section>

<section aria-label="Shared people application">
    <div data-people-app="true" data-api-base-url="@Model.PeopleApiBaseUrl" data-host-display-name="ModernHost.Mvc">
        <p class="hero__summary" role="status" aria-live="polite">Loading people directory...</p>
    </div>
</section>

<section class="cards" aria-label="Host details">
    <article class="card">
        <h2>Modern API configuration</h2>
        <p>The host resolves the shared people app backend from configuration and emits it through <code>data-api-base-url</code>.</p>
        <p class="endpoint">@Model.PeopleApiBaseUrl</p>
    </article>
</section>

@section Scripts {
    <script type="module" src="~/people-app/people-app.js" asp-append-version="true"></script>
}